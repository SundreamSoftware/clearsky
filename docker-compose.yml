services:
  clearsky-web:
    image: ghcr.io/sundreamsoftware/clearsky:latest
    container_name: clearsky-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:5010:80"
    volumes:
      - ./waqi-token.conf:/etc/nginx/waqi-token.conf:ro
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
